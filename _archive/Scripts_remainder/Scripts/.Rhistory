}
# Function to compute the moments \tau^n b(\tau)
moments <- function(n, dt = 0.0001, t0 = 0, t1 = 1, hitting_density_fun){
time_grid <- seq(t0, t1, by = dt)
f_t <- hitting_density_fun(time_grid)
index <- !is.nan(f_t)
t_n <- time_grid^n
b_t <- b_0(time_grid)
return(sum(t_n[index] * b_t[index] * f_t[index]*dt))
}
# Value function of the non-discounted problem ### IMPROVE THE PRECISION OF B
V_0 <- function(x){
if (x < 0.8399) {
return(sqrt(2*pi)*(1-0.8399^2)*exp(x^2/2)*pnorm(x))
} else {
return(x)
}
}
# Taylor expansion of the boundary with discount r evaluated at 0
V_r <- function(x, r, N, hitting_density_fun){
moments_N <- sapply(1:N, function(n) moments(n = n, hitting_density_fun = hitting_density_fun))
V <- sum( (-r)^(0:N)/factorial(0:N) * c(V_0(x), moments_N) )
return(V)
}
# Fixed point algorithm to find b_r
b_r <- function(r, tol = 1e-5, N, b_0, n) {
b_r <- 0
e <- 1
while (e > tol) {
b_r_old <- b_r
f_hitting <- hitting_density(t0 = 0, x0 = b_r_old, n = n, T = 1)
hitting_density_fun <- splinefun(seq(0, 1, by = T/n), f_hitting)
b_r <- V_r(x = b_r_old, r = r, N = N, hitting_density = hitting_density_fun)
e <- abs(b_r_old - b_r)
cat(paste0("error: ", e, " \n"))
}
return(b_r)
}
# Parameters
t0 <- 0
x0 <- 0
n <- 2000  # Number of time steps
T <- 1     # Upper bound of the interval
dt <- T / n  # Step size
r <- 1    # Discount
N <- 500 # Higher order in the Taylor expansion
time_grid <- seq(t0, T, by = dt)
# # Computing hitting density
# f_hitting <- hitting_density(t0 = t0, x0 = x0, n = n, T = T)
#
# # Check that f_hitting(t) integrates to 1
# sum(f_hitting[-(n+1)]*T/n)
#
# # Plot the result
# plot(time_grid, f_hitting, type = "l", col = "blue", lwd = 2,
#      xlab = "t", ylab = "f_hitting(t)",
#      main = "Solution of the First-Passage Time Density")
# Computing b_r(0) using the fixed point algorithm
b_r_0 <- b_r(r = r, tol = 1e-4, N = N, n = n)
b_r_0
# Boundary b_0 for the discounted case
b_0 <- function(t) { 0.8399 * sqrt(1 - t) }
# Derivative of b_0(t)
b_0_prime <- function(t) {
if (t == 1) return(Inf)  # Avoid division by zero at t = 1
return(-0.8399 / (2 * sqrt(1 - t)))
}
# Transition density Brownian bridge from (s, y) to (t, x)
f <- function(t, s, y) {
term <- sqrt(2 * pi * (1 - t) / (1 - s) * (t - s))
if (is.nan(term) || term == 0) return(0)
exponent <- -((b(t) - (1 - t) / (1 - s) * y)^2) / (2 * (1 - t) / (1 - s) * (t - s))
return(1 / term * exp(exponent))
}
# Volterra Kernel function K with singularity handling
K <- function(t, s, y) {
if (t == s) {
return(0) # Avoid singularity by returning 0 when t == s
}
term <- 0.5 * (b_0_prime(t) - (b_0(t) - y) / (t - s))
return(term * f(t, s, y))
}
hitting_density <- function(t0 = 0, x0 = 0, n = 5000, T = 1) {
if(x0 > b_0(x0)) return(c(1, rep(0, n)))
dt <- T / n # time steps
time_grid <- seq(t0, T, by = dt) # time grid
# Initialize solution for f_x0_t0(t)
dens <- numeric(n+1)
# Numerical integration (trapezoidal rule)
for (i in 3:(n+1)) {
cat(paste0("t = ", time_grid[i], "\r"))
t <- time_grid[i]
# Compute the kernel K(s, t0, x0) at this point
K_t <- K(t, t0, x0)
# Compute the integral part
u <- time_grid[2:(i-1)]
g_u <- dens[2:(i-1)]
K_u <- sapply(time_grid[2:(i-1)], function(s) K(t, s, b_0(s)))
integral_sum <- sum(g_u * K_u * dt)
# Update dens(t) using the integral equation
dens[i] <- -2 * K_t + 2 * integral_sum
}
return(dens)
}
# Function to compute the moments \tau^n b(\tau)
moments <- function(n, dt = 0.0001, t0 = 0, t1 = 1, hitting_density_fun){
time_grid <- seq(t0, t1, by = dt)
f_t <- hitting_density_fun(time_grid)
index <- !is.nan(f_t)
t_n <- time_grid^n
b_t <- b_0(time_grid)
return(sum(t_n[index] * b_t[index] * f_t[index]*dt))
}
# Value function of the non-discounted problem ### IMPROVE THE PRECISION OF B
V_0 <- function(x){
if (x < 0.8399) {
return(sqrt(2*pi)*(1-0.8399^2)*exp(x^2/2)*pnorm(x))
} else {
return(x)
}
}
# Taylor expansion of the boundary with discount r evaluated at 0
V_r <- function(x, r, N, hitting_density_fun){
moments_N <- sapply(1:N, function(n) moments(n = n, hitting_density_fun = hitting_density_fun))
V <- sum( (-r)^(0:N)/factorial(0:N) * c(V_0(x), moments_N) )
return(V)
}
# Fixed point algorithm to find b_r
b_r <- function(r, tol = 1e-5, N, b_0, n) {
b_r <- 0
e <- 1
while (e > tol) {
b_r_old <- b_r
f_hitting <- hitting_density(t0 = 0, x0 = b_r_old, n = n, T = 1)
hitting_density_fun <- splinefun(seq(0, 1, by = T/n), f_hitting)
b_r <- V_r(x = b_r_old, r = r, N = N, hitting_density = hitting_density_fun)
e <- abs(b_r_old - b_r)
cat(paste0("error: ", e, " \n"))
}
return(b_r)
}
# Parameters
t0 <- 0
x0 <- 0
n <- 2000  # Number of time steps
T <- 1     # Upper bound of the interval
dt <- T / n  # Step size
r <- 1    # Discount
N <- 500 # Higher order in the Taylor expansion
time_grid <- seq(t0, T, by = dt)
# Computing hitting density
f_hitting <- hitting_density(t0 = t0, x0 = x0, n = n, T = T)
# Check that f_hitting(t) integrates to 1
sum(f_hitting[-(n+1)]*T/n)
# Plot the result
plot(time_grid, f_hitting, type = "l", col = "blue", lwd = 2,
xlab = "t", ylab = "f_hitting(t)",
main = "Solution of the First-Passage Time Density")
# Computing b_r(0) using the fixed point algorithm
b_r_0 <- b_r(r = r, tol = 1e-4, N = N, n = n)
# Plot the result
plot(time_grid, f_hitting, type = "l", col = "blue", lwd = 2,
xlab = "t", ylab = "f_hitting(t)",
main = "Solution of the First-Passage Time Density")
T <- 1     # Upper bound of the interval
dt <- T / n  # Step size
r <- 1    # Discount
N <- 500 # Higher order in the Taylor expansion
time_grid <- seq(t0, T, by = dt)
# Computing hitting density
f_hitting <- hitting_density(t0 = t0, x0 = x0, n = n, T = T)
# Check that f_hitting(t) integrates to 1
sum(f_hitting[-(n+1)]*T/n)
# Boundary b_0 for the discounted case
b_0 <- function(t) { 0.8399 * sqrt(1 - t) }
# Derivative of b_0(t)
b_0_prime <- function(t) {
if (t == 1) return(Inf)  # Avoid division by zero at t = 1
return(-0.8399 / (2 * sqrt(1 - t)))
}
# Transition density Brownian bridge from (s, y) to (t, x)
f <- function(t, s, y) {
term <- sqrt(2 * pi * (1 - t) / (1 - s) * (t - s))
if (is.nan(term) || term == 0) return(0)
exponent <- -((b(t) - (1 - t) / (1 - s) * y)^2) / (2 * (1 - t) / (1 - s) * (t - s))
return(1 / term * exp(exponent))
}
# Volterra Kernel function K with singularity handling
K <- function(t, s, y) {
if (t == s) {
return(0) # Avoid singularity by returning 0 when t == s
}
term <- 0.5 * (b_0_prime(t) - (b_0(t) - y) / (t - s))
return(term * f(t, s, y))
}
hitting_density <- function(t0 = 0, x0 = 0, n = 5000, T = 1) {
if(x0 > b_0(x0)) return(c(1, rep(0, n)))
dt <- T / n # time steps
time_grid <- seq(t0, T, by = dt) # time grid
# Initialize solution for f_x0_t0(t)
dens <- numeric(n+1)
# Numerical integration (trapezoidal rule)
for (i in 3:(n+1)) {
cat(paste0("t = ", time_grid[i], "\r"))
t <- time_grid[i]
# Compute the kernel K(s, t0, x0) at this point
K_t <- K(t, t0, x0)
# Compute the integral part
u <- time_grid[2:(i-1)]
g_u <- dens[2:(i-1)]
K_u <- sapply(time_grid[2:(i-1)], function(s) K(t, s, b_0(s)))
integral_sum <- sum(g_u * K_u * dt)
# Update dens(t) using the integral equation
dens[i] <- -2 * K_t + 2 * integral_sum
}
return(dens)
}
# Function to compute the moments \tau^n b(\tau)
moments <- function(n, dt = 0.0001, t0 = 0, t1 = 1, hitting_density_fun){
time_grid <- seq(t0, t1, by = dt)
f_t <- hitting_density_fun(time_grid)
index <- !is.nan(f_t)
t_n <- time_grid^n
b_t <- b_0(time_grid)
return(sum(t_n[index] * b_t[index] * f_t[index]*dt))
}
# Value function of the non-discounted problem ### IMPROVE THE PRECISION OF B
V_0 <- function(x){
if (x < 0.8399) {
return(sqrt(2*pi)*(1-0.8399^2)*exp(x^2/2)*pnorm(x))
} else {
return(x)
}
}
# Taylor expansion of the boundary with discount r evaluated at 0
V_r <- function(x, r, N, hitting_density_fun){
moments_N <- sapply(1:N, function(n) moments(n = n, hitting_density_fun = hitting_density_fun))
V <- sum( (-r)^(0:N)/factorial(0:N) * c(V_0(x), moments_N) )
return(V)
}
# Fixed point algorithm to find b_r
b_r <- function(r, tol = 1e-5, N, b_0, n) {
b_r <- 0
e <- 1
while (e > tol) {
b_r_old <- b_r
f_hitting <- hitting_density(t0 = 0, x0 = b_r_old, n = n, T = 1)
hitting_density_fun <- splinefun(seq(0, 1, by = T/n), f_hitting)
b_r <- V_r(x = b_r_old, r = r, N = N, hitting_density = hitting_density_fun)
e <- abs(b_r_old - b_r)
cat(paste0("error: ", e, " \n"))
}
return(b_r)
}
# Parameters
t0 <- 0
x0 <- 0
n <-5000  # Number of time steps
T <- 1     # Upper bound of the interval
dt <- T / n  # Step size
r <- 1    # Discount
N <- 500 # Higher order in the Taylor expansion
time_grid <- seq(t0, T, by = dt)
# Computing hitting density
f_hitting <- hitting_density(t0 = t0, x0 = x0, n = n, T = T)
# Check that f_hitting(t) integrates to 1
sum(f_hitting[-(n+1)]*T/n)
# Plot the result
plot(time_grid, f_hitting, type = "l", col = "blue", lwd = 2,
xlab = "t", ylab = "f_hitting(t)",
main = "Solution of the First-Passage Time Density")
# Boundary b_0 for the discounted case
b_0 <- function(t) { 0.8399 * sqrt(1 - t) }
# Derivative of b_0(t)
b_0_prime <- function(t) {
if (t == 1) return(Inf)  # Avoid division by zero at t = 1
return(-0.8399 / (2 * sqrt(1 - t)))
}
# Transition density Brownian bridge from (s, y) to (t, x)
f <- function(t, s, y) {
term <- sqrt(2 * pi * (1 - t) / (1 - s) * (t - s))
if (is.nan(term) || term == 0) return(0)
exponent <- -((b(t) - (1 - t) / (1 - s) * y)^2) / (2 * (1 - t) / (1 - s) * (t - s))
return(1 / term * exp(exponent))
}
# Volterra Kernel function K with singularity handling
K <- function(t, s, y) {
if (t == s) {
return(0) # Avoid singularity by returning 0 when t == s
}
term <- 0.5 * (b_0_prime(t) - (b_0(t) - y) / (t - s))
return(term * f(t, s, y))
}
hitting_density <- function(t0 = 0, x0 = 0, n = 5000, T = 1) {
if(x0 > b_0(x0)) return(c(1, rep(0, n)))
dt <- T / n # time steps
time_grid <- seq(t0, T, by = dt) # time grid
# Initialize solution for f_x0_t0(t)
dens <- numeric(n+1)
# Numerical integration (trapezoidal rule)
for (i in 3:(n+1)) {
cat(paste0("t = ", time_grid[i], "\r"))
t <- time_grid[i]
# Compute the kernel K(s, t0, x0) at this point
K_t <- K(t, t0, x0)
# Compute the integral part
u <- time_grid[2:(i-1)]
g_u <- dens[2:(i-1)]
K_u <- sapply(time_grid[2:(i-1)], function(s) K(t, s, b_0(s)))
integral_sum <- sum(g_u * K_u * dt)
# Update dens(t) using the integral equation
dens[i] <- -2 * K_t + 2 * integral_sum
}
return(dens)
}
# Function to compute the moments \tau^n b(\tau)
moments <- function(n, dt = 0.0001, t0 = 0, t1 = 1, hitting_density_fun){
time_grid <- seq(t0, t1, by = dt)
f_t <- hitting_density_fun(time_grid)
index <- !is.nan(f_t)
t_n <- time_grid^n
b_t <- b_0(time_grid)
return(sum(t_n[index] * b_t[index] * f_t[index]*dt))
}
# Value function of the non-discounted problem ### IMPROVE THE PRECISION OF B
V_0 <- function(x){
if (x < 0.8399) {
return(sqrt(2*pi)*(1-0.8399^2)*exp(x^2/2)*pnorm(x))
} else {
return(x)
}
}
# Taylor expansion of the boundary with discount r evaluated at 0
V_r <- function(x, r, N, hitting_density_fun){
moments_N <- sapply(1:N, function(n) moments(n = n, hitting_density_fun = hitting_density_fun))
V <- sum( (-r)^(0:N)/factorial(0:N) * c(V_0(x), moments_N) )
return(V)
}
# Fixed point algorithm to find b_r
b_r <- function(r, tol = 1e-5, N, b_0, n) {
b_r <- 0
e <- 1
while (e > tol) {
b_r_old <- b_r
f_hitting <- hitting_density(t0 = 0, x0 = b_r_old, n = n, T = 1)
hitting_density_fun <- splinefun(seq(0, 1, by = T/n), f_hitting)
b_r <- V_r(x = b_r_old, r = r, N = N, hitting_density = hitting_density_fun)
e <- abs(b_r_old - b_r)
cat(paste0("error: ", e, " \n"))
}
return(b_r)
}
# Parameters
t0 <- 0
x0 <- 0
n <-10000  # Number of time steps
T <- 1     # Upper bound of the interval
dt <- T / n  # Step size
r <- 1    # Discount
N <- 500 # Higher order in the Taylor expansion
time_grid <- seq(t0, T, by = dt)
# Computing hitting density
f_hitting <- hitting_density(t0 = t0, x0 = x0, n = n, T = T)
# Check that f_hitting(t) integrates to 1
sum(f_hitting[-(n+1)]*T/n)
# Plot the result
plot(time_grid, f_hitting, type = "l", col = "blue", lwd = 2,
xlab = "t", ylab = "f_hitting(t)",
main = "Solution of the First-Passage Time Density")
data("fraud", package = "qss")
data("fraud", package = "qss")
data("russia2011", package = "qss")
## Question 1: Frequent Fractions in Vote Shares
## (a) Compute United Russia’s vote share as a proportion of voters who turned out
russia2011$vote_share <- russia2011$votes / russia2011$turnout
## (b) Identify the 10 most frequently occurring fractions for the vote share
freq_table <- table(russia2011$vote_share)
top_10_fractions <- sort(freq_table, decreasing = TRUE)[1:10]
print(top_10_fractions)
## (c) Create a histogram with bins for unique fractions
unique_fractions <- unique(russia2011$vote_share)
hist(russia2011$vote_share, breaks = length(unique_fractions),
main = "Histogram of Vote Shares", xlab = "Vote Share",
ylab = "Frequency")
## (d) Briefly describe the distribution
# Observations suggest high frequencies at fractions with low
# numerators and denominators, such as 1/2 and 1/3.
## Question 2: Monte Carlo Simulation
## (a) Assume a binomial distribution for turnout and vote shares
set.seed(123)
simulated_results <- numeric()
## (b) Conduct 1000 simulations
for (i in 1:1000) {
simulated_turnout <- rbinom(nrow(russia2011), size = russia2011$N,
prob = russia2011$turnout / russia2011$N)
simulated_votes <- rbinom(nrow(russia2011), size = simulated_turnout,
prob = russia2011$vote_share)
simulated_vote_share <- simulated_votes / simulated_turnout
simulated_results <- c(simulated_results, simulated_vote_share)
}
setwd("C:/Users/aguaz/OneDrive - CUNEF/Teaching/2024-25/2024-25 - 2do-C Quantitative Methods for Humanities - 2do PPE-A/Scripts")
#Load data
load(file = "qss_data/2_PROBABILITY/fraud.RData")
####
## Question 1: Frequent Fractions in Vote Shares
####
## (a) Compute United Russia’s vote share as a proportion of voters who turned out
russia2011$vote_share <- russia2011$votes / russia2011$turnout
## (b) Identify the 10 most frequently occurring fractions for the vote share
freq_table <- table(russia2011$vote_share)
top_10_fractions <- sort(freq_table, decreasing = TRUE)[1:10]
print(top_10_fractions)
## (c) Create a histogram with bins for unique fractions
unique_fractions <- unique(russia2011$vote_share)
hist(russia2011$vote_share, breaks = length(unique_fractions),
main = "Histogram of Vote Shares", xlab = "Vote Share",
ylab = "Frequency")
hist(russia2011$vote_share, main = "Histogram of Vote Shares", xlab = "Vote Share",
ylab = "Frequency")
####
## Question 2: Monte Carlo Simulation
####
## (a) Assume a binomial distribution for turnout and vote shares
set.seed(123)
simulated_results <- numeric()
####
## Question 2: Monte Carlo Simulation
####
## (a) Assume a binomial distribution for turnout and vote shares
set.seed(123)
simulated_results <- c()
####
## Question 2: Monte Carlo Simulation
####
## We assume a binomial distribution for turnout and vote shares
## (a) Conduct 1000 simulations
set.seed(123)
simulated_results <- c()
####
## Question 1: Frequent Fractions in Vote Shares
####
## (a) Compute United Russia’s vote share as a proportion of voters who turned out
russia2011$vote_share <- russia2011$votes / russia2011$turnout
## (b) Identify the 10 most frequently occurring fractions for the vote share
freq_table <- table(russia2011$vote_share)
top_10_fractions <- sort(freq_table, decreasing = TRUE)[1:10]
print(top_10_fractions)
russia2011$turnout
####
## Question 1: Frequent Fractions in Vote Shares
####
## (a) Compute United Russia’s vote share as a proportion of voters who turned out
russia2011$vote_share <- russia2011$votes / russia2011$turnout
russia2011$vote_share
## (c) Create a histogram with bins for unique fractions
unique_fractions <- unique(russia2011$vote_share)
unique_fractions
####
## Question 1: Frequent Fractions in Vote Shares
####
## (a) Compute United Russia’s vote share as a proportion of voters who turned out
russia2011$vote_share <- russia2011$votes / russia2011$turnout
## (b) Identify the 10 most frequently occurring fractions for the vote share
freq_table <- table(russia2011$vote_share)
top_10_fractions <- sort(freq_table, decreasing = TRUE)[1:10]
print(top_10_fractions)
## (c) Create a histogram with bins for unique fractions
unique_fractions <- unique(russia2011$vote_share)
hist(russia2011$vote_share, main = "Histogram of Vote Shares",
xlab = "Vote Share", ylab = "Frequency")
hist(russia2011$vote_share, breaks = length(unique_fractions),
main = "Histogram of Vote Shares" xlab = "Vote Share", ylab = "Frequency")
hist(russia2011$vote_share, breaks = length(unique_fractions),
main = "Histogram of Vote Shares", xlab = "Vote Share", ylab = "Frequency")
####
## Question 2: Monte Carlo Simulation
####
## We assume a binomial distribution for turnout and vote shares
## (a) Conduct 1000 simulations
set.seed(123)
simulated_results <- c()
for (i in 1:1000) {
simulated_turnout <- rbinom(nrow(russia2011), size = russia2011$N,
prob = russia2011$turnout / russia2011$N)
simulated_votes <- rbinom(nrow(russia2011), size = simulated_turnout,
prob = russia2011$vote_share)
simulated_vote_share <- simulated_votes / simulated_turnout
simulated_results <- c(simulated_results, simulated_vote_share)
}
## (c) Identify the 10 most frequent vote share values
simulated_freq_table <- table(simulated_results)
simulated_top_10 <- sort(simulated_freq_table, decreasing = TRUE)[1:10]
print(simulated_top_10)
## (d) Create a histogram similar to Question 1
unique_simulated <- unique(simulated_results)
hist(simulated_results, breaks = length(unique_simulated),
main = "Histogram of Simulated Vote Shares",
xlab = "Simulated Vote Share", ylab = "Frequency")
for (i in 1:100) {
simulated_turnout <- rbinom(nrow(russia2011), size = russia2011$N,
prob = russia2011$turnout / russia2011$N)
simulated_votes <- rbinom(nrow(russia2011), size = simulated_turnout,
prob = russia2011$vote_share)
simulated_vote_share <- simulated_votes / simulated_turnout
simulated_results <- c(simulated_results, simulated_vote_share)
}
## (c) Identify the 10 most frequent vote share values
simulated_freq_table <- table(simulated_results)
print(simulated_top_10)
## (d) Create a histogram similar to Question 1
unique_simulated <- unique(simulated_results)
gc()
